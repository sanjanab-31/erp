# Vite environment variables (copy to .env.local and fill values)
# Your Stripe publishable key (pk_test_...)
VITE_STRIPE_PUBLISHABLE_KEY=

# Payment server URL
VITE_PAYMENT_SERVER_URL=http://localhost:4242

# Optionally override API URL used by the app (if you wire it in)
# VITE_API_URL=http://localhost:5000/api
